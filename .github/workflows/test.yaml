---
name: test
on: pull_request
jobs:
  plan:
    timeout-minutes: 15
    runs-on: ubuntu-latest
    permissions: {}
    env:
      TFACTION_TARGET: foo
      TFACTION_WORKING_DIR: foo
      TFACTION_JOB_TYPE: terraform
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - uses: aquaproj/aqua-installer@5e54e5cee8a95ee2ce7c04cb993da6dfad13e59c # v3.1.2
        with:
          aqua_version: v2.62.1
        env:
          AQUA_GITHUB_TOKEN: ${{github.token}}

      - name: Generate token
        id: token
        uses: tibdex/github-app-token@3beb63f4bd073e61482598c45c71c1019b59b73a # v2.1.0
        with:
          app_id: ${{vars.APP_ID}}
          private_key: ${{secrets.APP_PRIVATE_KEY}}
          # pull_requests:write - Post comments
          # contents:write - Push commits
          permissions: >-
            {
              "pull_requests": "write",
              "contents": "write"
            }
          repositories: >-
            ["${{github.event.repository.name}}"]

      - name: Set up
        uses: suzuki-shunsuke/tfaction/setup@d0d52eb8b2ff13d6e27ac9653d038df384e58827 # v2.0.5
        with:
          github_token: ${{steps.token.outputs.token}}

      - name: Plan
        uses: suzuki-shunsuke/tfaction/plan@d0d52eb8b2ff13d6e27ac9653d038df384e58827 # v2.0.5
        with:
          github_token: ${{steps.token.outputs.token}}
        env:
          GITHUB_TOKEN: ${{steps.token.outputs.token}}
